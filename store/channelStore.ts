// Channel state is driven by URL routing (see hooks/useChannel.ts)
// Install zustand (pnpm add zustand) to add extra UI state here if needed.
//
// Example:
// import { create } from 'zustand'
// interface ChannelStore { activeChannel: string; setChannel: (id: string) => void }
// export const useChannelStore = create<ChannelStore>((set) => ({
//   activeChannel: 'home',
//   setChannel: (id) => set({ activeChannel: id }),
// }))

export {}
